import { moduleName } from './module'
import { createSelector } from 'reselect'

/**
* селекторы
*/
export const {{camelCase name}}State = state => state[moduleName]

//export const exampleSelector = createSelector(
// [{{camelCase name}}State],
// state => state.example
//)