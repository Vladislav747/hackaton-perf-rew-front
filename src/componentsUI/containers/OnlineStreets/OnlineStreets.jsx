import OnlineStreetsView from '../../views/OnlineStreets'
import { connect } from 'react-redux'

const mapDispatchToProps = dispatch => ({})

const mapStateToProps = state => ({})

export default connect(mapStateToProps, mapDispatchToProps)(OnlineStreetsView)
