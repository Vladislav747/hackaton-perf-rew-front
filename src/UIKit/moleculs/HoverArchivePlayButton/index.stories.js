import React from 'react'
import { storiesOf } from '@storybook/react'
import HoverArchivePlayButton from '.'

storiesOf('HoverArchivePlayButton', module)
    .add('default', () => <HoverArchivePlayButton onClick={()=>{}}/>)
