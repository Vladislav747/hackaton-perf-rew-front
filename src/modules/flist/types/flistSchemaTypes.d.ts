type getRelationSchemaObjectType = {
  RELATION_OBJECT?: string = "";
};

type getRelationSchemaRelationsType = {};

interface fullSelectedGroup {
  ID: number;
  NAME: string;
}

interface objectsState {
  parentId: string;
  RELATION_OBJECT: string;
}
