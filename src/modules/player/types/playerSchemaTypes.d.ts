type PlayerSchema = {
  volumeLevelStore: string;
  muteStatusStore: Boolean;
  currentVideoTime: number;
  newSelectedTime: number;
  userNeedToStartPlayManual: Boolean;
  speedCoefficient: number;
  isPlaying: Boolean;
  hdState: Boolean;
};
